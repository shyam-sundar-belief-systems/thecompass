# Database Configuration
# Get your PostgreSQL connection string from your database provider (Neon, Supabase, etc.)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# Optional: Production database URL (if different from development)
DATABASE_URL_PROD="postgresql://username:password@host:port/database?sslmode=require"

# NextAuth Configuration
# Generate a secure secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here"
# Set to your application URL (localhost for dev, production URL for prod)
NEXTAUTH_URL="http://localhost:3000"

# Supabase Configuration (if using Supabase features)
NEXT_PUBLIC_SUPABASE_URL="your-supabase-project-url"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"
